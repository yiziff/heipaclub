{
  "name": "cn-rap-cup-rank-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute cn-rap-cup-rank --file=./schema.sql",
    "db:init:local": "wrangler d1 execute cn-rap-cup-rank --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
