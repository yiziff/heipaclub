# Rank Worker（已合并）

排行榜已并入仓库根目录的主 Worker：[`../cf-api/index.js`](../cf-api/index.js) + 根目录 `wrangler.jsonc`（D1：`heipaclub-rank`）。

请看根目录 [README · 上线](../README.md#上线cloudflare)，不要再单独 `wrangler deploy` 本目录，除非你明确要拆成两个 Worker。
